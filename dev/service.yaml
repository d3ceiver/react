apiVersion: v1
kind: Service
metadata:
  name: my-react-app-service
spec:
  type: NodePort
  selector:
    app: my-react-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001  # Optional: specify a NodePort, or let Kubernetes choose one
